The following program answers the question whether or not a point is inside, outside on an edge or on a point of a polygon.

When run it should get the coordinates of the point which we have to observe.
After that - the number of points which the polygon is made.
And, finally, the coordinates of the points.